from setuptools import setup

setup(name='uswapper',
      version='0.14',
      description='Simple wrapper for uniswap graphql api',
      license='MIT',
      packages=['uswapper'],
      zip_safe=False)
