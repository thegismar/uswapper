from setuptools import setup

setup(name='uswapper',
      version='0.23',
      description='Simple wrapper for uniswap graphql api',
      license='MIT',
      packages=['uswapper'],
      zip_safe=False)
